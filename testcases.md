# ECMP Test Cases

| Test Case ID | Test Case Name | Objective | Setup | Test Steps | Expected Results | Actual Result | Automation |
|--------------|----------------|-----------|-------|------------|------------------|---------------|------------|
| 2.4.1 | ECMP Basic Functionality (Static) | Validate that static routes with equal-cost next-hops are installed correctly and traffic is distributed among them. | 3 Nodes | 1. Configure two static routes with the same prefix but different next hops.<br>2. Generate traffic and verify load balancing.<br>3. Shut one next-hop and verify traffic continuity through remaining path. | 1. Both next-hops are active and installed in the routing table (`show ip route`).<br>2. Load balancing observed.<br>3. Fast convergence after link failure. | Pass / Fail / Partial Pass | Yes |
| 2.4.1 | ECMP Scaling (Static) | Validate performance and scalability with many ECMP routes and next hops. | 4 Nodes | 1. Configure ECMP routes with varying next-hop counts (4, 8, 16).<br>2. Add 1L ECMP prefixes.<br>3. Monitor CPU/memory utilization.<br>4. Verify traffic distribution.<br>5. Trigger next-hop failure and measure failover time. | 1. All ECMP routes installed successfully (`show ip route`).<br>2. System resources within limit.<br>3. No packet loss; quick failover. | Pass / Fail / Partial Pass | No |
| 2.4.3 | ECMP Negative Testing (Static) | Ensure system rejects invalid ECMP configurations and maintains stability during failure. | 3 Nodes | 1. Configure ECMP with duplicate or unreachable next hops.<br>2. Delete the next hop while traffic is flowing.<br>3. Observe CLI and syslog messages.<br>4. Re-add next-hop and check recovery.<br>5. Configure more than supported ECMP next-hops (e.g., 70 when limit = 64). | 1. Invalid inputs are rejected with proper error.<br>2. Traffic continues via remaining paths.<br>3. System recovers cleanly on re-configuration.<br>4. Only supported number (e.g., 64) are programmed into routing table; remaining entries are either rejected or not installed.<br>5. Syslog includes "ECMP group size exceeds supported limit" (or vendor-equivalent). | Pass / Fail / Partial Pass | No |
| 2.4.4 | ECMP Basic Functionality (OSPF) | Validate OSPF ECMP route installation and forwarding behavior. | 4 Nodes (1 DUT, 3 OSPF neighbors with equal cost) | 1. Configure OSPF on all nodes with same cost to destination.<br>2. Send continuous traffic and monitor load distribution.<br>3. Shut one neighbor and verify reroute to remaining ones. | 1. ECMP entries appear in routing table using `show ip route ospf`.<br>2. Equal load balancing observed.<br>3. Fast convergence after link failure. | Pass / Fail / Partial Pass | Yes |
| 2.4.5 | ECMP Scaling (OSPF) | Validate system scalability with large number of OSPF ECMP routes. | 1 DUT + 4 OSPF Neighbors | 1. Advertise a large number of equal-cost prefixes from multiple OSPF neighbors.<br>2. Monitor CPU and memory.<br>3. Trigger adjacency flap and observe recovery. | 1. All ECMP routes installed using `show ip route ospf`.<br>2. No resource overuse.<br>3. Smooth recovery from neighbor flaps. | Pass / Fail / Partial Pass | No |
| 2.4.6 | ECMP Basic Functionality (BGP) | Validate ECMP route installation and forwarding for BGP-learned prefixes. | 4 Nodes (1 DUT + 3 BGP peers advertising same prefix with same AS path length) | 1. Configure iBGP/eBGP sessions with multiple equal-cost peers.<br>2. Advertise identical prefixes with same attributes.<br>3. Enable BGP multipath on DUT.<br>4. Send traffic and confirm load balancing.<br>5. Shutdown one BGP peer and check traffic reroute. | 1. BGP routes installed as ECMP entries (`show ip bgp`).<br>2. Equal traffic distribution.<br>3. Smooth reroute after peer failure. | Pass / Fail / Partial Pass | Yes |
| 2.4.7 | ECMP Scaling (BGP) | Validate scalability for large-scale BGP ECMP routes. | 1 DUT + 4 BGP Peers | 1. Enable BGP multipath feature.<br>2. Advertise large number of identical prefixes from multiple peers.<br>3. Verify all equal-cost paths are installed.<br>4. Monitor CPU/memory and BGP session stability.<br>5. Introduce peer flap and observe convergence. | 1. System handles multiple ECMP entries.<br>2. No route loss or instability.<br>3. Fast failover observed. | Pass / Fail / Partial Pass | No |
| 2.4.8 | ECMP Negative Testing (Dynamic Routing) | Validate stability and correct rejection of invalid ECMP paths in OSPF/BGP. | 1 DUT + 3 Routing Peers | 1. Advertise same prefix with different metric in OSPF (non-equal cost).<br>2. Advertise prefixes in BGP with different AS path lengths.<br>3. Observe if only valid ECMP routes are installed.<br>4. Induce route flap and verify no loops or blackhole.<br>5. Advertise the same prefix from many peers with identical attributes to exceed supported equal paths (e.g., 70). | 1. Only equal-cost paths are used for ECMP.<br>2. No loops or inconsistent routing.<br>3. System logs errors properly for invalid paths.<br>4. Only supported number (e.g., 64) are programmed into routing table; remaining entries are either rejected or not installed. | Pass / Fail / Partial Pass | No |
